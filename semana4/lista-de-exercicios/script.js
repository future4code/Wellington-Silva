/*terça*/

/*Exercicio 1 - interpretação de código

1. A função moeda recebe um parametro valorEmDolar,
seguinte é guardado o valor recebido do DOM e depois
retornamos a conversão dos valores.

Depois colocamos essa função dentro de uma constante,
com o valor 100 em seu parametro, por fim imprimimos 
a variavel no console.log.

2. Nessa função, temos dois parametros que é o tipo
de investimento e valor a investir, logo após temos uma variavel que ira guardar o valor depois
do investimento.

Depois entramos no switch que são os tipos de investimentos
e seu rendimento que mostra o valor que será colocado multiplicado
pelo rendimento.

E retornaremos esse switch na variavel criada para ele,
que é valorAposInvesimento.

Depois temos duas constantes que guardam a função mais o valor que
quer ser aplicado e por fim temos duas impressões das constantes.

3. Começamos com 3 constantes que recebem array, uma que possui
valores e a duas outra que são vazias.

Por fim ,temos um for que faz a verificação, se o valor que esta
dentro do array é par e será colocado no array1 e se não for sera
colocado no array2

no final será impresso em um console.log a quantidade total
de valores, em outra os numeros pares e na outra o equivalente 
a numeros impares

4.


*/



/*Exercicio 2 - Logica de programação*/

/*item 1
const booleano1 = true
const booleano2 = false
const booleano3 = !booleano2 
const booleano4 = !booleano3 

a. será valor false

b. será valor false

c. será valor true

d. será valor true

e. será valor true


item 2

a.	

b.

c.

d. function comparador(){
	let number1 = Number(prompt('Digite um numero'))
	let number2 = Number(prompt('Digite o segundo numero: '))


	
	if (number1 > number2 || number1%number2===0) {
		console.log ('O maior é '+ number1)
		console.log (number1+' é divisivel '+number2)
		console.log ('A diferença entre eles é: '+(number1-number2))

	}else if(number1 > number2 || number1%number2){
		console.log ('O maior é ' +number2)
		console.log (number1+' não é divisivel '+number2)
		console.log ('A diferença entre eles é: '+(number1-number2))
	}

}

let conta = comparador();
console.log(comparador());

*/
